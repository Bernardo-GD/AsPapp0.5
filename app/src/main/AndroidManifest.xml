<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pachoncito.aspapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AsPapp">
        <activity android:name=".ConsejosCategoria"></activity>
        <activity android:name=".AmorometroIndividual" />
        <activity android:name=".CalendarioActividad" />
        <activity android:name=".ConsejosPrincipal" />
        <activity android:name=".CalendarioPrincipal" />
        <activity android:name=".AmorometrosPrincipal" />
        <activity android:name=".RegistroP2" />
        <activity android:name=".RegistroP1" />
        <activity
            android:name=".MainActivity"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>